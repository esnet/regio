name: ht_datapath_stats

blocks:
  stats: &stats
    !include stats.yaml

interfaces:
  - block: *stats
    address: 0x0000
    name: flow_pktbyte_count
  - block: *stats
    address: 0x1000
    name: flow_pktsize_histogram
